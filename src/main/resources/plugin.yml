name: Harmony
version: '${project.version}'
main: com.harmony
api-version: 1.17
prefix: Harmony
load: STARTUP
authors: [ Caizii ]
description: 调整一个服务器，从各个方面
softdepend:
  - MMOItems

commands:
  harmony:
    description: 查看插件基本信息
    usage: /<command>

  gameover:
    description: 执行自定义游戏事件
    usage: /<command>